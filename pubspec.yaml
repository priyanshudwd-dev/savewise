name: savewise
description: "SaveWise - your friendly money companion"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5
  intl: ^0.20.3
  timezone: ^0.9.4
  path_provider: ^2.1.4
  fl_chart: ^0.69.0
  flutter_local_notifications: ^17.2.3
  share_plus: ^10.1.2
  file_picker: ^8.1.2

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
